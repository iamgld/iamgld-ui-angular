import { SelectInputErrorMessageDirective } from './select-input-error-message.directive';

describe('SelectInputErrorMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectInputErrorMessageDirective();
    expect(directive).toBeTruthy();
  });
});
