import { InputErrorMessage } from './input-error-message.directive'

describe('InputErrorMessage', () => {
  it('Given the symbol is imported, When the test runs, Then it is defined', () => {
    expect(InputErrorMessage).toBeDefined()
  })
})
