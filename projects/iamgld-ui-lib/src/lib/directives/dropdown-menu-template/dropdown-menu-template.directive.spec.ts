import { DropdownMenuTemplate } from './dropdown-menu-template.directive'

describe('DropdownMenuTemplate', () => {
  it('Given the symbol is imported, When the test runs, Then it is defined', () => {
    expect(DropdownMenuTemplate).toBeDefined()
  })
})
